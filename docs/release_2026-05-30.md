# Release 2026-05-30 - Dashboard ejecutivo agua

## Artefactos

| Archivo | Destino | Tamano | SHA-256 |
| --- | --- | ---: | --- |
| `dist\agua-dashboard-sync-agent.zip` | Computadora de la cooperativa | Ver archivo generado | Ver `dist\checksums.sha256` |
| `dist\agua-dashboard-vps-release.zip` | VPS | Ver archivo generado | Ver `dist\checksums.sha256` |
| `dist\checksums.sha256` | Control de integridad | 205 bytes | Archivo de hashes |

## Verificacion ejecutada

Comando:

```powershell
powershell -ExecutionPolicy Bypass -File O:\agua_dashboard\scripts\verify_release.ps1 -ProjectRoot O:\agua_dashboard -SkipBuild -BrowserSmoke
```

Resultado:

- tests backend/sync: `25 passed`;
- `django check`: correcto;
- paquete cooperativa regenerado;
- paquete VPS regenerado;
- checksums escritos;
- checksums verificados;
- smoke API correcto con `source_mode: mysql`;
- smoke visual correcto con capturas moviles.

Capturas:

```text
C:\Users\Ventas\AppData\Local\Temp\agua-dashboard-browser-smoke\dashboard-mobile.png
C:\Users\Ventas\AppData\Local\Temp\agua-dashboard-browser-smoke\collections-day-mobile.png
```

## Incluido

- Dashboard ejecutivo Vue/Django.
- Sincronizador portable para computadora de la cooperativa.
- Release VPS con frontend compilado.
- Templates Linux para `systemd`, `gunicorn` y Nginx.
- Template SQL para usuarios MySQL `sync_writer` y `dashboard_reader`.
- Smoke tests de API y navegador.
- Verificador integral de release.
- Verificador de checksums.
- Documentacion de despliegue, validacion gerencial y entrega operativa.

## Pendientes externos para aceptacion final

- Instalar `agua-dashboard-sync-agent.zip` en la computadora de la cooperativa.
- Completar `.env` real del agente y ejecutar `verify.ps1`.
- Ejecutar una sincronizacion real desde la cooperativa y confirmar log `status: ok`.
- Registrar tarea programada en esa computadora.
- Desplegar `agua-dashboard-vps-release.zip` en VPS con dominio HTTPS.
- Ejecutar smoke API contra el dominio final.
- Validar indicadores contra reportes actuales de gerencia.
